Q1. Basic Git Workflow
git init
git branch -M main

cat > README.md <<EOF
This is a first file
EOF

git add README.md
git commit -m "[Krishna] add: First file"

git remote add origin https://github.com/DivyanshuTenguriya/Demo.git
git remote -v
git push -u origin main

cat > README.md <<EOF
Readme file
EOF

git add README.md
git commit -m "[Krishna] add: First change"

git status
ls


Q2. Branching and Merging

git branch feature-a
git checkout feature-a

cat > server.js <<EOF
// server file content
EOF

ls
git add server.js
git commit -m "[Krishna] add: server file"
git push
git push -u origin feature-a

git checkout main
git checkout -b feature-b

cat > server.js <<EOF
// server file content for feature-b
EOF

git add server.js
git commit -m "[Krishna] add: server file"
git push -u origin feature-b
git status

git checkout feature-a
git merge feature-b


Q3. Conflict Resolution

git checkout main
git checkout -b branch1

echo "Change from branch1" >> README.md
git add README.md
git commit -m "[Krishna] add: Branch1 changes"

git checkout main
git checkout -b branch2

echo "Change from branch2" >> README.md
git add README.md
git commit -m "[Krishna] add: Branch2 changes"

git checkout main
git merge branch1
git merge branch2

# Resolve conflict inside README.md manually:
# Final content should be:
# Change from branch1
# Change from branch2

git add README.md
git commit -m "[Krishna] fix: merge conflict resolved"


Q4. GitHub Collaboration

git clone https://github.com/yashchaudhary1605/Frontend-Dev.git
cd Frontend-Dev

git checkout -b new-feature

echo "Contribution update" >> contribution.txt

git add .
git commit -m "[Krishna] add: contribution update"
git push origin new-feature


Q5. Advanced Workflow

mkdir advanced-git
cd advanced-git

git init

touch app.py config.json notes.txt
git add .
git commit -m "[Krishna] add: Initial project files"

touch .gitignore
echo "*.log" >> .gitignore
echo "__pycache__/" >> .gitignore

git add .gitignore
git commit -m "[Krishna] add: gitignore"

echo "Temporary work" >> app.py
git stash
git stash pop

git tag -a v1.0 -m "[Krishna] First stable release"

git remote add origin https://github.com/your-username/advanced-git.git
git push origin main
git push origin --tags
